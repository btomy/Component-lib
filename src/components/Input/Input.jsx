import React from 'react';

const Input = ({
    disabled,
    required,
    type,
    id,
    placeholderText,
    hintText,
    validationText,
    labelText
}) =>
    <div>

    </div>

export default Input;
